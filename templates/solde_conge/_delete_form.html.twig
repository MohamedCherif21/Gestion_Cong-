<form method="post" action="{{ path('app_solde_conge_delete', {'id': solde_conge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solde_conge.id) }}">
    <button class="btn">Delete</button>
    
</form>
